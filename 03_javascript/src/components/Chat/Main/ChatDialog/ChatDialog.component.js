import React, { PureComponent as Component } from 'react';
import './ChatDialog.styles.css';

class CharDialog extends Component {
    render() {
        const { isDialog, name, unread } = this.props;

        return ( 
           null
        );
    }
};

export default CharDialog;